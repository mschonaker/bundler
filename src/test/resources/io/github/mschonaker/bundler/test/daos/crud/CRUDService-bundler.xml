<?xml version="1.0" encoding="UTF-8" ?>
<bundler>
	<createTables><![CDATA[
		
		create table users (
			username varchar(200) not null primary key,
			password varchar(20) not null,
			realname varchar(200) not null
		);
		
	]]></createTables>

	<find>select * from users where username = ${param}</find>

	<upsert>merge into users(username, password, realname)
		values(${param.username}, ${param.password}, ${param.realname})
	</upsert>

	<count>select count(*) from users</count>

	<delete>delete from users where username = ${param}</delete>

</bundler>